@model QuestionDetailsViewModel
@inject SignInManager<IdentityUser> signInManager;

@{
    ViewData["Title"] = "Details";
}
<div>
    <h1>@Html.DisplayFor(model => model.Question.QuestionName)</h1>
    <br />

    <dl class="row">
        <dt class="col-sm-2">Possible Answers</dt>
    </dl>

    <table class="table">

        @foreach (var answer in Model.Answers)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => answer.AnswerName)
                </td>
            </tr>
        }
    </table>

    <hr />

    <dl class="row">
        <dt class="col-sm-2">Correct Answer</dt>
    </dl>
    <p>@Html.DisplayFor(model => model.Answers.FirstOrDefault(x => x.IsCorrect).AnswerName)</p>
</div>

@if (signInManager.IsSignedIn(User) && User.IsInRole("Admin"))
{
<div>
    <a asp-action="Edit" asp-route-id="@Model.Question.Id">Edit</a> |
    <a asp-controller="Quizzes" asp-action="Details" asp-route-id="@Model.Question.QuizId">Back to Questions</a> |
    <a asp-controller="Quizzes" asp-action="Index">Back to Quiz List</a>
</div>
}
else
{
<div>
    <a asp-controller="Quizzes" asp-action="Details" asp-route-id="@Model.Question.QuizId">Back to Questions</a> |
    <a asp-controller="Quizzes" asp-action="Index">Back to Quiz List</a>
</div>
}
